
export function loadN64(romBuffer, container) {
  container.innerHTML = '<p>🕹 N64 emulator launched (stub). Replace with Mupen64Plus Web init.</p>';
  // TODO: Wire up Mupen64Plus with romBuffer
}
